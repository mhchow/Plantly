import React from 'react';
import styled from 'styled-components';

const StyledModal = styled.div`
  background-color: lightblue;
`
const Modal = () => {
  return (

    <StyledModal>
      This is will be an input form
    </StyledModal>
  )
}

export default Modal;